#!/usr/bin/env bash

alias startSSHTunnels='/home/n1ko23/Scripts/startSSHTunnels.sh' # Starts the ssh tunnels to remote server
alias stopSSHTunnels='/home/n1ko23/Scripts/stopSSHTunnels.sh'   # Closes the ssh tunnels to remote server

alias open='_open_files_for_editing'     # opens given file(s) for editing
alias pacdiff=_Pacdiff

alias ls='ls --color=auto'       # gotta have them colors when doing ls :trollface:
alias ll='ls -lav --ignore=..'   # show long listing of all except ".."
alias l='ls -lav --ignore=.?*'   # show long listing but no hidden dotfiles except "."

# To deal with bs systemctl commands
alias ssr='sudo systemctl restart'
alias ss='systemctl status'
alias sst='sudo systemctl stop'
alias sss='sudo systemctl start'
alias sse='sudo systemctl enable'
alias ssd='sudo systemctl disable'

alias start2B2WTunnel='sudo systemctl start tunnel2b2t.service'
alias stop2B2WTunnel='sudo systemctl stop tunnel2b2t.service'
